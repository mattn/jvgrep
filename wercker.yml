box: wercker/default
steps:
  - wercker/step-github-create-release
deploy:
    steps:
        - script:
            name: get version
            code: export APP_VERSION=$(make && ./jvgrep -V)
        - github-create-release:
            token: $GITHUB_TOKEN
            tag: $APP_VERSION

