box: wercker/default
steps:
  - motemen/github-create-release@1.0.4
deploy:
    steps:
        - script:
            name: get version from rake
            code: export APP_VERSION=$(./jvgrep -V)
        - github-create-release:
            token: $GITHUB_TOKEN
            tag: $APP_VERSION
